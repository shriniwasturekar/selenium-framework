# browser defaults
browser=chrome
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# selenium
retry.count=1
headless=false

# reporting
screenshot.on.failure=true

# framework
base.path=/login
